#This repo should only ever contain non-sensitive information related to SAFETAG framework development.

# Audit content should not ever be committed
/audit/*

#Report or ADIDS content should never be committed.
/build/*

#Don't commit any auto-saves
\#*\#
*~

#Ignore Latex Modules
modules/install-tl*
modules/texlive

#Ignore all fonts that are downloaded
styles/fonts
NotoSans-hinted.zip

# New

node_modules
meta/.*
